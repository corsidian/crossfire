# Set this to the path where crossfire lives.
# If this is not set the script will look for /usr/local/crossfire, then
# /opt/crossfire.
#crossfire_HOME=""

# If there is a different user you would like to run crossfire as,
# change the following line.
#crossfire_USER="daemon"

# If you wish to change the location of the crossfire pid file,
# change the following line.
#crossfire_PIDFILE="/var/run/crossfire.pid"

# If you wish to explictly specific the location of the log directory,
# you can set it here.  Note that this applies to the logs generated outside
# crossfire itself.  If you want to change the location of crossfire's own
# logs, see the system property 'log.directory'.  If this is not set,
# it will default to $crossfire_HOME/logs.
#crossfire_LOGDIR="/some/where/logs"

# If you wish to set any specific options to pass to the JVM, you can
# set them with the following variable.
#crossfire_OPTS="-Xmx1024m"

# If you wish to override the auto-detected JAVA_HOME variable, uncomment
# and change the following line.
#JAVA_HOME=/usr/java/default
